---
# Docker integration — lets Homepage read container labels for service discovery.
# Containers can opt in with homepage.* labels; see homepage docs for full schema.
local:
  socket: /var/run/docker.sock
